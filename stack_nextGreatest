vector<int> nextGreater(vector<int>& arr){
    stack<int> a;
    vector<int> b(arr.size(), -1);
    for(int i = arr.size() - 1; i >= 0; i--){
        while(!a.empty() && a.top() < arr[i]) a.pop();
        if(!a.empty()) b[i] = a.top();
        a.push(arr[i]);
    }
    return b;
}
